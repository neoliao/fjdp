package ${packagePrefix}.dao;

import ${packagePrefix}.model.${modelName};
import net.fortunes.core.dao.GenericDao;

public class ${modelName}Dao extends GenericDao<${modelName},${idType}> {

}
